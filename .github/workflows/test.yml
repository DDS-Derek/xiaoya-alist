name: test

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: |
            shellcheck main.sh
            bash main.sh test