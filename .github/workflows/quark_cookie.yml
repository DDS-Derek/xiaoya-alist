name: quark cookie exe

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - "quark_cookie/**"
      - ".github/workflows/quark_cookie.yml"

jobs:
  Windows-amd64:
    runs-on: windows-latest
    name: Build Windows Binary
    steps:
      - 
        name: Checkout
        uses: actions/checkout@v4

      - 
        name: Init Python 3.12
        uses: actions/setup-python@v5
        with:
            python-version: '3.12'
            cache: 'pip'

      - 
        name: Install Dependent Packages
        run: |
            pip install wheel pyinstaller
            pip install -r requirements.txt
        shell: pwsh
        working-directory: ./quark_cookie

      - 
        name: Pyinstaller
        run: |
            pyinstaller quark_login.spec
        working-directory: ./quark_cookie
        shell: pwsh

      - 
        name: Upload Windows File
        uses: actions/upload-artifact@v3
        with:
            name: quark_login-windows-amd64
            path: quark_cookie/dist/quark_login.exe

  Linux-amd64:
    runs-on: ubuntu-latest
    name: Build Linux Amd64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Init Python 3.12
        uses: actions/setup-python@v5
        with:
            python-version: '3.12'
            cache: 'pip'

      - name: Install Dependent Packages
        run: |
            python -m pip install --upgrade pip
            pip install wheel pyinstaller
            pip install -r requirements.txt
        working-directory: ./quark_cookie

      - name: Pyinstaller
        run: |
            pyinstaller quark_login.spec
            mv dist/quark_login dist/quark_login-linux-amd64
        working-directory: ./quark_cookie

      - name: Upload Linux File
        uses: actions/upload-artifact@v3
        with:
            name: quark_login-linux-amd64
            path: quark_cookie/dist/quark_login-linux-amd64

  macos-amd64:
    runs-on: macOS-latest
    name: Build macOS Amd64
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Init Python 3.12
        uses: actions/setup-python@v5
        with:
            python-version: '3.12'
            cache: 'pip'
    
      - name: Install Dependent Packages
        run: |
            python -m pip install --upgrade pip
            pip install wheel pyinstaller
            pip install -r requirements.txt
        working-directory: ./quark_cookie

      - name: Pyinstaller
        run: |
            pyinstaller quark_login.spec
            mv dist/quark_login dist/quark_login-macos-amd64
        working-directory: ./quark_cookie
            
      - name: Upload macOS File
        uses: actions/upload-artifact@v3
        with:
            name: quark_login-macos-amd64
            path: quark_cookie/dist/quark_login-macos-amd64

  Update:
    runs-on: ubuntu-latest
    needs: [ Windows-amd64, Linux-amd64, macos-amd64]
    steps:
      - 
        name: Checkout
        uses: actions/checkout@v4

      - 
        name: Download Artifact
        uses: actions/download-artifact@v3

      - 
        name: Rename
        shell: bash
        run: |
            mv ./quark_login-windows-amd64/quark_login.exe ./quark_login/quark_login-windows-amd64.exe
            mv ./quark_login-macos-amd64/quark_login-macos-amd64 ./quark_login/quark_login-macos-amd64
            mv ./quark_login-linux-amd64/quark_login-linux-amd64 ./quark_login/quark_login-linux-amd64

      - 
        uses: stefanzweifel/git-auto-commit-action@be823a7e51f116fecebc222b8307716921375992
        with:
            commit_message: 'chore: update quark_login windows exe'
            branch: master
            file_pattern: './quark_login/*'
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
